program POrder;

uses
  Forms,
  project_order in 'project_order.pas' {ProjectOrderForm};

{$R *.res}

begin
  Application.Initialize;
  Application.CreateForm(TProjectOrderForm, ProjectOrderForm);
  Application.Run;
end.
